# PR-01 foundation only. No database, LINE, Stripe, or provider secrets are
# introduced in this PR. Real secrets must never be committed to this file
# or to any file in this repository.

# Shared across all apps (see packages/config)
NODE_ENV=development
LOG_LEVEL=info
