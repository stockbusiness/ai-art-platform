/**
 * Placeholder only. Real LIFF ID Token exchange (POST /api/v1/auth/line/exchange)
 * is implemented in Phase 2 — this PR must not connect to the LINE SDK.
 */
export function AuthCallbackPage() {
  return <p>LIFF authentication is not implemented in this build.</p>;
}
